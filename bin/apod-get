#!/usr/bin/env bash

set -eu

prgname=${0##*/}

path="$HOME/.cache/apod/latest"

case $prgname in
    apod-get)
        curl -sSL --create-dirs -o "$path" "$(apod)" || exit 1
        ;;
    apod-bg)
        feh --bg-max --no-fehbg "$path"
        ;;
esac
