#!/usr/bin/env bash

set -eu

if (( $EUID )); then
    echo 'This script must be run with root privileges.' >&2
    exit 1
fi

wpa_passphrase "$1" | sed -e '1i\\' -e '/^\s*#/d;s/\t/    /g' \
    >>/etc/wpa_supplicant/wpa_supplicant.conf
