#!/usr/bin/env bash

set -eu

prgname=${0##*/}

cd ~/tmp

config=~/.config/vim/test1.vim

vim -u "$config"
tmux -L "$prgname" new vim -u "$config"

nvim -u "$config"
tmux -L "$prgname" new nvim -u "$config"
