#!/bin/bash

set -e

check() {
    echo $'\n--------------------------------------------------'
    echo "$*"
    echo -e "$("$1" --version)\n"
    "$@"
}

if [ -n "$VIRTUAL_ENV" ]; then
    check pip list
fi

check /usr/bin/pip list --user
check /usr/bin/pip2 list --user
echo
