#!/usr/bin/env bash

set -eu -o pipefail

prgname=${0##*/}

if [[ $prgname = 'dmenu1' ]]; then
    dmenu "$@" | head -n1
    exit
fi

exec /usr/bin/dmenu -nb '#3a3a3a' -sb '#000000' \
                    -nf '#e4e4e4' -sf '#87cefa' \
                    -fn 'local_terminal_sized' \
                    -i -p "${1:->}"
