#!/usr/bin/env bash

export BASH_ENV=~/bin/shx.sh

# FIXME: removed exec as a workaround for when BASH_ENV is otherwise ignored
# e.g., weird case of systemd/tmux/nvim/systemlist(cmd, input)
bash "$@"
