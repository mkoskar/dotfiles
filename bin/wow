#!/bin/bash

set -e

cd ~/projects/notes/src/09-policy

output() {
    local files=([0-9]*) l=${#files[@]}
    for i in "${!files[@]}"; do
        cat "${files[i]}"
        ((i != l-1)) && echo
    done
}

output | PAGER_TITLE='Words Of Wisdom' pg -c 'set ft=rst fdm=marker'
