" vim: ft=vim fdm=marker

let vimrc = get(g:, 'vimrc', expand('<sfile>:p'))
source ~/.vim/vimrc_base

set nobackup
set noswapfile
set noundofile
set undolevels=-1
set viminfo=

" ----------------------------------------

Plugin 'gnupg'

let GPGDefaultRecipients = ['CDC91131']
let GPGUsePipes = 1

" ----------------------------------------

exec VimrcFinalize()
