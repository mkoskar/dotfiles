#!/usr/bin/env bash

set -eu

check() {
    echo $'\n--------------------------------------------------'
    echo "$*"
    echo -e "$("$1" --version)\n"
    "$@"
}

if [[ ${VIRTUAL_ENV-} ]]; then
    check pip list
fi

check /usr/bin/pip list --user
check /usr/bin/pip2 list --user
echo
