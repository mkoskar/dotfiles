#!/bin/bash

set -eu

sdu() { systemctl --user "$@"; }

if [ "$(basename "$0")" = 'mbsync-off' ]; then
    sdu stop mbsync@head.timer
    sdu stop mbsync@tail.timer
    exit
fi

sdu start mbsync@head.timer
sdu start mbsync@tail.timer

# trigger head
sdu start mbsync@head
