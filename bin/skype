#!/usr/bin/env bash

# set -eu
#
# cleanup() {
#     local pids
#     pids=($(jobs -p))
#     (( ${#pids[@]} )) && kill "${pids[@]}"
# }
# trap cleanup EXIT
#
# [[ -t 1 ]] || exec 2>/dev/null
#
# skyped -s -n -H 127.0.0.1 &
# /usr/bin/skype

exec /usr/bin/skype
