#!/usr/bin/env bash

set -eu

prgname=${0##*/}

path="$HOME/.cache/apod/latest"

case $prgname in
    apod-get)
        url=$(apod)
        curl -sSL --create-dirs -o "$path" "$url" || exit 1
        ;;
    apod-bg)
        feh --bg-max --no-fehbg "$path"
        ;;
esac
