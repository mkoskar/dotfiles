#!/usr/bin/env bash

set -eu

prgname=${0##*/}

if (( ! $# )); then
    echo "usage: $prgname cmd [arg]..." >&2
    exit 2
fi

. ~/bin/term.sh

if [[ ${TERMDONE-} && ! $TERM =~ ^(screen|tmux)(-|$) ]]; then
    # wait a bit for WM geometry to kick-in
    sleep 0.05
fi

exec "$@"
