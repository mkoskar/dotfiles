[Unit]
Description=%N service

[Service]
SyslogIdentifier=%N
ExecStart=/usr/bin/weechat-headless \
    -P alias,buflist,charset,fset,irc,logger,perl,python,relay,script,spell,trigger
Restart=always

[Install]
WantedBy=default.target
