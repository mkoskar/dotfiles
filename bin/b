#!/usr/bin/env bash

set -eu

prgname=${0##*/}

if [[ $prgname = 'b0' ]]; then
    args=(ffp -p quarantine -- --private-window)
else
    args=("${BBIN:-ff}" --)
fi

exec "${args[@]}" "$@" &>/dev/null &
