#!/usr/bin/env bash

set -eu

prgname=${0##*/}

if (( ! $# )); then
    echo "usage: $prgname cmd [arg]..." >&2
    exit 2
fi

[[ -e ~/bin/term.sh ]] && . ~/bin/term.sh

if [[ ${TERM%%-*} != 'screen' ]]; then
    # wait a bit for WM geometry to kick-in
    sleep 0.05
fi

exec "$@"
