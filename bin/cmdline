#!/usr/bin/env bash

set -eu

pid=${1:-$PPID}

[ "$pid" -gt 0 ] 2>/dev/null || {
    echo "usage: ${0##*/} pid" >&2
    exit 2
}

infile="/proc/$pid/cmdline"
[[ -e $infile ]] && xargs -0 -x -r -a "$infile" shell-escape
