#!/bin/bash

if [ "$(xserverq name)" = 'Xorg.bin' ]; then
    if [ "$(basename "$0")" = 'xscreen-mobile' ]; then
        autorandr -l mobile
    else
        autorandr -c -d mobile --force
    fi
fi

xset b off s off dpms 0 0 300
xsetroot -solid black
xsetroot -cursor_name left_ptr
