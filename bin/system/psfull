#!/usr/bin/env bash

set -eu

# ps -eFly
# ps -eo s,uid,pid,ppid,c,pri,ni,rss,sz,wchan,psr,stime,tty,time,cmd

ps -eo 'pid,ppid,sess,pgid,user,pri,ni,vsz:7,rss:7,stat,%cpu,%mem,stime,time,tt=TTY,wchan:25,unit:25,cmd' "$@" | $PAGER
