#!/bin/bash

main() {
    osdd &
    twmnd &
    workrave &
    xmonad
}

xmonad() {
    tray
    /usr/bin/xmonad
}

openbox() {
    tint2 &
    /usr/bin/openbox
}

xscreen

[ $# -gt 0 ] && eval "$*"
