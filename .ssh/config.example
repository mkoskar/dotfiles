# ~/.ssh/config

Host <host>
Hostname <hostname>
User <user>

Host *
HashKnownHosts yes
ControlMaster auto
ControlPath /tmp/.%r@%h:%p

# Can resolve some connection delays.
#GSSAPIAuthentication no

# Only for servers which are accessed by secure network!
#Ciphers arcfour
