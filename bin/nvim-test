#!/usr/bin/env bash

set -eu

prgname=${0##*/}

cd ~/tmp

config=~/.config/nvim/init-test.vim

vim -u "$config"
tmux -L "$prgname" new vim -u "$config"

nvim -u "$config"
tmux -L "$prgname" new nvim -u "$config"
