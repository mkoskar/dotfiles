#!/usr/bin/env bash

set -eu

prgname=${0##*/}

case $prgname in
    modules-builtin)
        $PAGER </lib/modules/$(uname -r)/modules.builtin
        ;;
    *)
        $PAGER </lib/modules/$(uname -r)/modules.order
        ;;
esac
