#!/usr/bin/env bash

set -eu -o pipefail

{
    paccheck --quiet \
        --files --file-properties \
        --md5sum --sha256sum --require-mtree \
        --db-files --backup --noextract --noupgrade || true
} | sort
