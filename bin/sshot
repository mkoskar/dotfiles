#!/usr/bin/env bash

set -eu -o pipefail

cd ~/tmp

g="$(slop -n -f '%g\n' --opengl --magnify)"
maim -g "$g" "screenshot-$(date +%F.%s).png"

find -maxdepth 1 -type f -name 'screenshot-*' | sort -r | sxiv -io
