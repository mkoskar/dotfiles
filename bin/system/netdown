#!/usr/bin/env bash

set -eu

if (( $EUID )); then
    echo 'This script must be run with root privileges.' >&2
    exit 1
fi

cd /sys/class/net

for i in *; do
    [[ $i != 'lo' ]] || continue
    ip link set dev "$i" down
done
