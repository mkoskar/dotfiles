[Unit]
Description=apod service
Requires=dbus.socket
After=dbus.socket

[Service]
Type=simple
SyslogIdentifier=apod
ExecStart=/bin/bash -lc 'apod-get && { xdisplay apod-bg || true; }'
Restart=on-failure
RestartSec=1800
