#!/usr/bin/env bash

set -eu

prgname=${0##*/}

if (( ! $# )); then
    echo "usage: $prgname user [<git clone options>]" >&2
    exit 2
fi

user=$1
shift

github-repos "$user" | parallel git clone "$@"
