#!/bin/bash

set -e

tmpfile=$(mktemp)
cleanup() { rm -f "$tmpfile"; }
trap cleanup EXIT

/usr/bin/sxiv -o "$@" >"$tmpfile"

[ -s "$tmpfile" ] || exit 0

mkdir .marked
xargs -d '\n' -r -I{} -a "$tmpfile" mv {} -t .marked
