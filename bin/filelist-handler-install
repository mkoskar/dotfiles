#!/bin/bash

set -e

if [ $# -eq 0 ]; then
    echo "usage: $(basename "$0") target"
    exit 2
fi

target="$1"

[ ! -w "$target" -o -x "$target" ] && exit 1

sed -i '1i#!/usr/bin/env filelist-handler' "$1"
chmod a+x "$target"
