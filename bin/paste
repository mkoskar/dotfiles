#!/usr/bin/env bash

set -eu

prgname=${0##*/}

case $prgname in
    paste-ix)
        exec curl -sLF "f:1=@${1:--}" http://ix.io/
        ;;
    paste-ptpb)
        # syntax highlighting: <url>/<pygments-lexer>
        exec curl -sF "c=@${1:--}" -w '%{redirect_url}\n' -o /dev/null 'https://ptpb.pw/?r=1'
        ;;
    paste-sprunge)
        # syntax highlighting: <url>?<pygments-lexer>
        exec curl -sLF "sprunge=@${1:--}" http://sprunge.us/
        ;;
    *)
        paste-ptpb "$@"
        ;;
esac
