#!/usr/bin/env bash

set -eu

prgname=${0##*/}

if (( ! $# )); then
    echo "usage: $prgname host" >&2
    exit 2
fi

sdu() { systemctl --user "$@"; }

if [[ $prgname = 'ssh-tunnel-off' ]]; then
    sdu stop "ssh-tunnel@$1"
    exit
fi

sdu start "ssh-tunnel@$1"
