#!/bin/bash

set -e

event=$1
session=$XSESSION

startup() {
    case $session in
        main)
            wmname LG3D
            xup
            ;;
    esac
}

case $event in
    startup)
        startup
        ;;
esac
