#!/usr/bin/env bash

set -eu

prgname=${0##*/}

if (( ! $# )); then
    echo "usage: $prgname cmd" >&2
    exit 2
fi

if [[ $prgname = 'ptha' ]]; then
    type -Pa -- "$1"
else
    type -P -- "$1"
fi
