#!/usr/bin/env bash

set -eu -o pipefail

state=$(status wifi | awk '{print $2}')
case $state in
    on)
        rfkill block wifi
        ;;
    off)
        rfkill unblock wifi
        ;;
esac

status-osd wifi
