#!/usr/bin/env bash

set -eu

prgname=${0##*/}

if (( ! $# )); then
    echo "usage: $prgname cmd [arg]..." >&2
    exit 2
fi

if [[ ! -t 1 ]]; then
    exec bashx -c "$(shell-escape "$@")"
fi

CMD=$(shell-escape "$@") PAGER_TITLE=${PAGER_TITLE:-"$ $CMD"} pg -c ReloadX
