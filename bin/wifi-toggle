#!/usr/bin/env bash

set -eu -o pipefail

state=$(rfkill-state -a wlan)
case $state in
    -1 | 1)
        rfkill-state wlan 0
        ;;
    0)
        rfkill-state wlan 1
        ;;
esac

status-osd wifi
