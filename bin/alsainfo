#!/usr/bin/env bash

{
    infohdr - amixer info

    infohdr - amixer -c 0 info

    infohdr /proc/asound headcat /proc/asound/{cards,devices,modules,pcm,version}

    infohdr - aplay -l

    infohdr - arecord -l

    infohdr - aplay -L

    infohdr - lsof +c 0 +D /dev/snd

    echo
} |& $PAGER
