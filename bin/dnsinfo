#!/usr/bin/env bash

echo

fqdn=$(hostname -f)
aliases=$(hostname -a)

printf 'DNS host name (FQDN):   %s\n' "$fqdn"
printf 'Aliases:                %s\n' "$aliases"

if hash resolvconf &>/dev/null; then
    echo $'\n--------------------------------------------------'
    echo $'> resolvconf:\n'

    resolvconf -l
    tail -vn +1 /etc/resolvconf.conf
fi

echo $'\n--------------------------------------------------\n'

tail -vn +1 /etc/resolv.conf
echo
