#!/usr/bin/env bash

set -eu

prgname=${0##*/}

if (( ! $# )); then
    echo "usage: $prgname host" >&2
    exit 2
fi

if [[ $prgname = 'ssh-tunnel-off' ]]; then
    systemctl --user stop "ssh-tunnel@$1"
    exit
fi

systemctl --user start "ssh-tunnel@$1"
