#!/usr/bin/env bash

set -eu

prgname=${0##*/}

if [[ ! -t 0 ]]; then
    set -- "$@" -
fi

if [[ $prgname = 'e0' ]]; then
    set -- -u ~/.vim/vimrc_base "$@"
fi

VIMBIN=${VIMBIN:-vim}
type -P "$VIMBIN" &>/dev/null || VIMBIN='vim'

exec "$VIMBIN" -O "$@"
