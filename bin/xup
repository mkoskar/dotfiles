#!/bin/bash

set -e

s() {
    local sname=$1
    local wname="s_$sname"
    xdotool search --classname "$wname" >/dev/null \
        || { term "$wname" - trun s "$sname" & }
}

s wrk
sleep 0.3

s tmp
sleep 0.3

s aux

wait
