#!/bin/bash

set -e

if [ $(id -u) -ne 0 ]; then
    echo 'This script must be run with root privileges.'
    exit 1
fi

nft list tables | while read table; do
    echo
    nft list "$table"
done

echo
