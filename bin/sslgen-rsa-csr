#!/bin/bash

set -e

openssl genrsa -des3 -out site.key 4096
#openssl genpkey -algorithm RSA -pkeyopt rsa_keygen_bits:4096 -des3 -pass stdin -out site.key

openssl req -new -sha256 -key site.key -out site.csr
cat site.csr
