#!/usr/bin/env bash

set -eu

event=$1
session=$XSESSION

startup() {
    case $session in
        main)
            apod-bg &
            srun -x clementine
            xup &
            ;;
    esac
}

case $event in
    startup)
        startup
        ;;
esac

wait
