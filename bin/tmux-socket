#!/bin/bash

[ -z "$TMUX" ] && exit 1

IFS=, <<<"$TMUX" read -ra var
socket=${var[0]##*/}

if [ "$(basename "$0")" = 'tmux-socket-label' ]; then
    [ ! "$socket" = 'default' ] && echo "$socket " || true
else
    echo "$socket"
fi
